# 设计记录

## 风格定位

深色创作工作台，界面强调可操作、可扫描、少说明文字。默认 JOJO 版偏蓝色公司 IM 工具感；网红短剧版保留微信聊天与短剧剪辑工具气质。

## 色彩

- 背景：`--app-bg #07130d`，卡片面：`--app-surface #101f17`。
- 主色：微信绿 `--wechat-green #07c160`，用于主按钮、焦点、成功状态。
- JOJO 版通过 `.app-shell-jojo` 覆盖为钉钉蓝：主色 `#1677ff`，背景 `#07130d`，编故事浮层使用低饱和深蓝毛玻璃。
- JOJO 版蓝底按钮使用白色文字/图标；网红短剧版保持原微信绿和深色按钮字。
- 辅色：紫 `--app-purple #b779ff`、橙 `--app-orange #f6ad55`、警示黄 `--app-warning #f2c94c`。
- 文本：主文本 `#f2f7f4`，弱文本 `#9fb5a8`，更弱文本 `#6f8a7b`。

## 布局

- 顶部固定工具栏 `.topbar`，无底部分割线、无额外背景，和页面背景拉通；左侧显示品牌标题，随后依次排列文件、显示、角色、模型和帮助菜单，右侧不放当前模型或额外状态文字。
- 点击顶部菜单直接展开对应选项；`⌘K` 打开“模型”，`⌘S` 保存存档，`⌘I` 载入存档。版本互跳位于“文件”，支持作者和关于本站位于“帮助”。
- 微信版原有“多会话（测试版）”开关迁移到“显示”菜单，以勾选状态呈现；生成中保持禁用，避免改变正在执行的请求拓扑。
- AI 模型选择沿用 `.settings-option-row`，使用自定义深色列表框强化选中态但不新增强调色；默认豆包 Seed-2.0-mini（速度快），顺序为豆包、DeepSeek V4 Flash、自定义模型。只有选中自定义模型时才展开接口模板、Base URL、模型名、API Key 和测试按钮，移动端保持无横向溢出。
- Beta、正式微信和正式钉钉顶部统一使用贴合页面的传统 macOS 菜单栏：高度 `40px`，横栏容器保持透明，不使用独立描边、圆角、背景模糊或投影；网站标题左侧显示品牌头像，微信版固定绿色气泡，钉钉正式版与 Beta 随机使用 JOJO 角色头像。文件、显示、角色、模型和帮助保持纯文字，语言并入“显示”，当前项以左侧勾选标记。下拉菜单仍保留独立承载面和轻投影，保证内容层级清晰。“模型”不打开弹窗，直接显示豆包、DeepSeek、Fish 朗读和单行“自定义 Fish Audio API”输入/测试。顶部右侧不重复显示当前模型文字，390px 窄屏将模型菜单固定在右侧 `8px`，避免裁切。
- 网红短剧版的性别选择使用小图标与文字横向排列，使用细边界但不使用方形图标底框；“支持鼓励”中的收款码只裁切显示二维码本体和必要白边。
- 微信版预制库由 13 个大众同人梗和 1 个经典长线小说式开场组成；覆盖《西游记》《喜剧之王》、西门庆、国际时事、AI 科技圈与中文互联网人物，经典款保留《房东把你推给我了》。首条西门庆与武大郎对手戏扩展为完整的一轮连续包袱；峰哥拥有成年女粉暧昧私聊和男 B 友情感答疑两条独立脚本。角色偏好默认“不限”，允许同性或非情感对手戏；主动选择男、女后只进入对应情感视角脚本池。
- 非恋爱预制脚本使用独立的 6 张网红感小动物插画头像：由一张 3×2 成套图等分裁切，包括丑橘猫、水豚、柴犬、小熊猫、羊驼和海豹。
- README 使用 `docs/screenshots/fakechat-wechat.webp` 作为微信版产品截图，截图保持真实运行界面，不额外加展示框。
- 主工作区 `.workspace.static-workspace` 以预览内容为中心，右侧预览负极容器固定按视频宽度占位，避免界面版/视频版切换时横向跳动。
- 桌面端界面版手机壳采用接近 iPhone 的窄长比例；1080px 以下把编故事面板改为底部居中半弹窗，400px 以下仅让模拟聊天手机轻微等比缩小，不再切成 16:9 或改变整体结构。
- 桌面端左侧“编故事”在没有故事卡时使用 `--desktop-story-panel-top-offset: 190px`；第一张待生成或已完成故事卡出现后，整栏通过现有 GSAP 布局过渡上移，顶部与右侧模拟页面的 `12px` 顶边对齐。
- 1080px 以下进入底部编故事交互：默认展开为半弹窗，展开态按钮是向下箭头，收起后变为底部居中的钢笔圆按钮，再点击钢笔恢复完整面板；收起按钮父级预留阴影空间，避免阴影和过渡被裁切；生成成功后自动收起开始看故事。
- 卡片统一 `.surface-card`；JOJO 版左侧卡片使用深蓝毛玻璃、轻微角落光感和白色高光描边。
- 右侧预览容器 `.preview-wrap` 是透明无边承载区，不使用整块填充背景；预览切换按钮跟随预览内容，居中放在内容上方。
- 视频版空态使用 `.video-empty-frame`，保持和 Remotion Player 一样的 16:9 画布宽高，只在画布内显示弱占位文案。
- JOJO 版手机壳使用 Figma 导出的钉钉顶栏和底部：`public/dingtalk-ui/topbar.webp` 来自 Figma `3:14`，`public/dingtalk-ui/inputbar.webp` 来自底部资源。
- JOJO 版中间聊天区左右分布：叫叫作为玩家永远右侧蓝色气泡；铃铛、猪小弟、系统在左侧白色气泡。

## 动效

- AI 模型列表框展开使用 `180ms` 的透明度、轻微上移和缩放入场，收起保留 `160ms` 退场状态；箭头同步旋转。`prefers-reduced-motion` 下停用动画和过渡。
- 全版本顶部菜单展开使用 `200ms` 克制的透明度、位移与缩放，收起保留 `140ms` 退场；不使用弹簧回弹。生成/导出百分比使用 Calligraph `variant="number"`、`animation="smooth"` 和 `18ms` 数位错峰，增减时逐位纵向滚动。
- 首屏进入：`.motion-in` 使用 GSAP，`y: 18 -> 0`、透明和模糊渐入，时长 `0.55s`。
- 交互悬停：按钮、链接、Prompt 卡片轻微上浮 `y: -2`、`scale: 1.01`。
- 按下反馈：`scale: 0.985`，保持轻量。
- 聊天重播按钮 `.chat-replay-button` 放在聊天信息流末尾，文案为“再来一遍”，弱灰色居中；只在播放完成后出现，只重播当前会话入场，不改变剧情内容。
- 启动默认剧情时先显示完整手机框架，再逐条播放消息入场。
- 新气泡入场后自动滚动 `.wechat-chat-scroll`，保证最新消息曝光在手机可视区域内。
- 界面版/视频版切换使用淡入淡出和轻微缩放/模糊，不再做左右滑动。
- 预览内容在桌面端支持鼠标位置驱动的轻微 3D 斜切；触摸设备和减少动效设置下关闭。
- 夜草地背景的入场缩放只做慢速减速，不出现越界回弹。
- PNG 存档封面独立绘制为 300×400：顶部显示当前聊天对象名字和双方真实头像，中间使用约 18 个无文字的绿白或蓝白气泡云，底部保留存档标题和域名；只保留轻暗角，不使用鱼眼或色散。
- 1080px 以下编故事面板展开/收起使用底部半弹窗动效；按钮通过 `document.startViewTransition` 与 `view-transition-name: story-panel-toggle` 做 continuity transition，从弹窗右上角连续移动到底部居中。

## 可复用样式

- 面板滚动：`.panel-scroll`。
- 底部半弹窗收起按钮：`.story-panel-status`，由 `storyPanelOpen` 控制展开/收起；展开态用向下箭头，收起态用钢笔图标，1080px 以下启用 continuity transition。
- 卡片容器：`.surface-card`、`.card-header`、`.card-content`。
- 编故事输入框：`.prompt-textarea` 在 JOJO 版使用纯黑背景。
- 预览容器：`.preview-wrap`、`.wechat-preview-shell`、`.player-frame`；`.wechat-preview-shell` 左对齐承载手机。
- 微信预览顶底图：`.wechat-topbar-img` 使用 `topbar.webp`，`.wechat-bottombar-img` 使用 `bottombar.webp`；联系人名用 `.wechat-topbar-title` 叠在顶栏中间。
- 钉钉预览顶底图：`.dingtalk-topbar-img`、`.dingtalk-inputbar-img`；聊天区 `.dingtalk-chat-scroll`。
- JOJO 气泡：`.dingtalk-row-self .wechat-bubble` 为蓝底白字，`.dingtalk-row-other .wechat-bubble` 为白底深色字。
- 品牌图标：微信版使用绿色圆底、单一白色聊天气泡和粗黑轮廓；钉钉正式版与 Beta 使用叫叫、铃铛、猪小弟的 6 个夸张表情圆形头像并在每次刷新时随机选择一次。素材统一为 `512×512` 压缩 WebP、圆外透明；favicon、`.brand-logo` 与 `.beta-menu-brand-icon` 必须保持同图。正式版为 `30px`、Beta 为 `24px`，均为 `50%` 圆角；Safari favicon 加单次刷新标记避免复用旧缓存。功能菜单不放装饰图标，只保留选中勾和加载状态。
- 状态文案：`.deepseek-status` 只在错误时展示，正常待机不显示 AI 模型常驻状态；生成中和成功提示使用当前模型名称。免费模型拥堵时显示明确错误，不自动切换到付费模型。

## JOJO 素材审美

- JOJO 群名默认偏同事“蛐蛐小群”语气，如“工位蛐蛐小队”；DeepSeek 续写时可生成 4-10 字的轻松群名，避免正式公司群名称。
- 头像严格使用 `avatar-jojo` 源图，不让生成模型重画角色脸。
- 公司照片使用真实办公室局部抓拍感，允许手、背影、运动模糊、电脑、咖啡、走廊、电梯口，不出现可识别正脸或卡通角色脸。
- 表情包使用本地头像组合成蓝色职场表情卡，优先保证角色形状准确。

## 状态反馈

- AI 模型正常待机时不常驻显示状态条，避免占用创作区视觉焦点。
- AI 模型或生成链路失败时才显示错误状态，文案需要明确“未连通/本地续写”等降级信息。
- 全版本 Toast 出现后不主动倒计时；用户没有鼠标移动或点击时持续保留，发生交互后以最后一次交互为准延迟 `2000ms` 消失。
- 内网部署使用同域 Fastify 服务，页面状态和生成请求优先走 `/api/settings/deepseek` 与 `/api/story/continue`。
- 静态 `/ququ/` 部署在外网访问且公司 token 中转不可达时，使用右上 Toast 提示 `token服务连不上，请连到叫叫的 Wi-Fi`，随后降级本地续写。
